using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class apagaritem : MonoBehaviour {
	

	// Use this for initialization
	void Start () {
		
		Invoke ("apaga", 2.5f);
	}
	
	void apaga () {

		Destroy (this.gameObject);
	}
}
